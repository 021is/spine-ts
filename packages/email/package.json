{
  "name": "@021/spine-email",
  "version": "0.1.0",
  "description": "Resend wrapper with structured logging, retries, and a uniform send API. Templates live in the consuming app.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": { ".": { "types": "./dist/index.d.ts", "import": "./dist/index.js" } },
  "files": ["dist"],
  "scripts": { "build": "tsup src/index.ts --format esm --dts --clean", "typecheck": "tsc --noEmit" },
  "dependencies": {
    "@021/spine-errors": "workspace:*"
  }
}
